# Used when building the client for production (npm run build)
# Same origin deploy (API serves frontend): leave empty or use relative paths
# VITE_API_URL=

# Separate frontend host (e.g. Vercel) pointing at your API:
# VITE_API_URL=https://api.your-app.example.com
